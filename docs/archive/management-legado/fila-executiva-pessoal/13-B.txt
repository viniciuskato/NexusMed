PROMPT 13-B — Completar, validar em CI real e publicar a suíte crítica

MODO DE EXECUÇÃO
Ao receber este texto, assuma imediatamente o papel de sessão executiva e execute-o neste próprio contexto. Não pergunte se deve executar, revisar o prompt, validar premissas ou assumir outro papel: comece a execução. Esta ordem já foi liberada pela diretoria. Reporte objetivamente o que foi feito ao final (comandos rodados, resultado, números e erros), sem narrar o raciocínio passo a passo nem pedir validação intermediária além do que for explicitamente exigido. Entregue o `RETORNO: 13-B` e encerre.

OBJETIVO
Concluir as lacunas de maior risco deixadas pelo 13-A, validar o workflow em runner real do GitHub Actions e, somente com todos os gates verdes, integrar e publicar a infraestrutura de testes. Reaproveitar integralmente a arquitetura e as fixtures já criadas; não redesenhar a suíte.

EFICIÊNCIA RESULTADO/TOKENS
Busque sempre a melhor relação resultado/tokens: reutilize contexto, helpers, fixtures e provas válidas; evite reler, reexecutar ou documentar o que não mudou. Aprofunde apenas o que altera risco, decisão ou critério de publicação. Não economize tokens omitindo validações críticas, evidências de segurança ou gates obrigatórios.

ESTADO CONHECIDO
- Clone canônico: `C:\Users\vinic\dev\NexusMed\firebase-auth`.
- Branch candidata: `work/13a-suite-critica`.
- Commit local informado: `4bf8dce`.
- 13-A entregue parcialmente: Vitest 15/15, pgTAP 183/183 e Playwright 10/10 em duas execuções completas após reset local.
- CI criada em duas camadas: `fast` em push/PR e `full` com Supabase + Playwright antes de merge; permissões `contents: read`, sem segredos e com artefatos sanitizados.
- Fixtures `e2e-13a-*` tiveram limpeza comprovada após sucesso e falha induzida.
- `npm run verify` permanece verde e o 13-A não tocou `main` nem remoto.

LACUNAS A FECHAR
1. Duas abas/contextos para reação, nota e progresso de leitura. Favorito já possui cobertura parcial; ampliar somente se o helper compartilhado tornar isso barato ou se for necessário para provar isolamento/concorrência.
2. Revisão SRS concorrente/idempotente.
3. Rascunho e término idempotente de simulado.
4. Execução real dos workflows no GitHub Actions.

DECISÕES DA DIRETORIA SOBRE OS ACHADOS
- Status inesperado: a garantia fail-closed está correta. Nesta entrega, alinhar somente comentário, documentação e expectativa do teste ao comportamento intencional (`status` desconhecido é normalizado para `pending` e nunca libera o app). Não redesenhar a tela nem alterar autenticação, salvo se um teste reproduzir concessão indevida.
- Questão offline: registrar como dívida de UX. O estado converge, mas pode ficar até ~20 s sem feedback imediato. Não implementar UI otimista nem alterar `QuestionCard`/fila nesta entrega; preservar teste de convergência eventual e abrir item futuro mensurável.

ESCOPO
1. Confirmar caminho, branch, HEAD, `main`, `origin/main`, working tree, worktrees e concorrência; fazer fetch seguro.
2. Revisar o diff integral do 13-A contra o `origin/main` vigente, incluindo lockfile, scripts, fixtures, sanitização de artefatos e YAML dos workflows.
3. Se `origin/main` avançou, reconciliar na branch candidata. Parar em conflito semântico ou autoria incerta.
4. Implementar somente os testes faltantes listados, reutilizando helpers/fixtures existentes e aguardas por condições observáveis. Não usar sleeps fixos ou retries globais para esconder flakiness.
5. Executar a suíte completa duas vezes a partir de banco local resetado; provar zero fixtures após sucesso e após falha controlada apenas se os novos caminhos introduzirem limpeza diferente. Não repetir provas caras já demonstradas sem motivo.
6. Validar `npm run verify`, testes unitários, pgTAP, Playwright, build, `git diff --check` e ausência de dados/segredos em logs, traces e screenshots.
7. Para validar runner real, é autorizado enviar somente a branch candidata e abrir/usar PR de validação. Não enviar `main` antes dos gates. Verificar `fast` e `full`, permissões, artefatos e condição que torna o gate completo obrigatório antes do merge.
8. Se o workflow falhar por defeito de código/configuração, corrigir minimamente na candidata e repetir. Se falhar apenas por limitação externa de runner, cota ou permissão não concedida, parar e reportar; não enfraquecer o gate.
9. Com revisão e CI reais verdes, integrar em `main` sem force, enviar `main`, acompanhar o deploy e executar smoke não destrutivo. Se a mudança não altera bundle/runtime, ainda confirmar build/deploy e disponibilidade do aplicativo.
10. Atualizar `AGENTS.md`, `docs/diretoria/registro.md` e o registro executivo persistente com cobertura real, limitações e hashes publicados.

RESTRIÇÕES
- Nenhuma escrita no Supabase remoto; fixtures e testes somente no Supabase local/runner isolado.
- Não corrigir UI offline, refatorar autenticação, resolver warnings alheios, alterar produto ou ampliar cobertura além das lacunas definidas.
- Não expor tokens, e-mails reais, `.env`, service role ou dados de estudo em workflow/artefatos.
- Sem force push, reset destrutivo ou descarte de trabalho concorrente.
- Parar antes do merge se qualquer gate estiver vermelho, se o CI completo não for realmente obrigatório, se houver segredo/PII em artefato ou se `origin/main` avançar durante o gate final.

CRITÉRIOS DE CONCLUSÃO
- Cenários concorrentes de reação, nota, progresso, SRS e simulado versionados e verdes.
- Suíte completa reproduzida duas vezes localmente sem resíduos.
- Workflows `fast` e `full` executados em runner real; falha impede merge de forma comprovada ou por configuração de proteção verificável.
- Branch integrada/publicada somente após todos os gates.
- Achado de status documentado como comportamento intencional fail-closed; atraso offline registrado, não mascarado nem corrigido fora de escopo.

FORMATO DO RETORNO
RETORNO: 13-B
Resultado: publicado | não publicado | bloqueado
Estado inicial/final: caminho, branches, HEADs, origin/main, working tree e worktrees
Revisão do 13-A: escopo, dependências, workflows e sanitização
Cobertura complementar: cenários, arquivos, assertivas e resultados
Execuções locais: contagens, duração, resets e limpeza
CI real: runs, jobs, eventos, permissões, gates e artefatos
Achados de comportamento: tratamento documental do status e registro da UX offline
Integração/publicação: commits, PR, merge, push, deploy e smoke
Dados de teste: evidências de isolamento e zero resíduos
Pendências e riscos: somente itens objetivos
Liberação das próximas frentes: sim | não, com motivo
