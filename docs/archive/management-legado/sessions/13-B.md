# Sessão 13-B

- Estado: PUBLICADA
- Prompt: `C:\Users\vinic\OneDrive\Organização\NexusMed-Prompts-Executivos\13-B.txt`
- Branch candidata: `work/13a-suite-critica`
- Base declarada: `4bf8dce`
- Autorização especial: ligar o fluxo real de `FlashcardReviewSession` à RPC atômica existente, após teste de reprodução; sem migration ou mudança de RPC/RLS sem nova decisão.

## Objetivo

Fechar os cenários concorrentes, validar `fast` e `full` em runner real e publicar somente com todos os gates verdes.

## Retorno declarado

- `main`/`origin/main`: `0c7834a` → `42252b9`.
- Achado de escopo (autorizado): `FlashcardReviewSession.tsx` nunca usava a RPC `submit_flashcard_review` — calculava SRS no cliente e fazia upsert cego; religada. `FlashcardReviewer.tsx` (código morto) removido.
- 7 specs novos em `concorrencia-13b.spec.ts`, dois `BrowserContext` reais.
- Achado paralelo: duas abas do mesmo `BrowserContext` competem sem trava na fila local (`syncQueue`) — não corrigido, registrado como dívida técnica.
- CI: 5 pushes, 4 achados de infra corrigidos (import morto; Node 20→22; corrida `supabase db reset` × pgTAP; formato de saída do `supabase status`). Nenhum tocou RPC/RLS/migration.
- fast+full 100% verdes no run final (pgTAP 183/183, Playwright 17/17 real).
- Integração: merge `--no-ff` (`af1dbd4`), verify pós-merge limpo, push, deploy Vercel confirmado por hash de bundle, smoke de produção sem erros/dados criados.
- Pendências declaradas: corrida entre abas do mesmo contexto na fila de sync; acessibilidade de teclado do `AdminCMSView` (herdada do 12-B).
- Documentação: `AGENTS.md` e `docs/diretoria/registro.md`, entrada "13-B".

## Verificação independente

Feita com ferramentas próprias no repositório `C:\Users\vinic\dev\NexusMed\firebase-auth` (não apenas aceitando o relato):

- `main` local = `origin/main` = `42252b9`; working tree limpo; `git worktree list` só mostra o próprio repositório (sem worktree paralelo).
- `af1dbd4` é merge real de dois pais (`0c7834a` + `5cef8a4`) e é ancestral de `main` (`git merge-base --is-ancestor` OK).
- Histórico de `main` mostra exatamente os commits declarados: `4bf8dce` (suíte), `89caa8c` (fecha lacunas + religa RPC), `77b444e`/`50d6ed1`/`815e1b9`/`0ae5e5a` (4 fixes de CI), `5cef8a4`/`af1dbd4`/`42252b9` (docs + merge + docs de publicação).
- Os 4 commits de fix de CI tocam só `.github/workflows/ci.yml`, `scripts/run-db-tests.mjs` e o próprio spec (remoção de 1 linha de import) — confirmado que nenhum mexeu em RPC/RLS/migration.
- `FlashcardReviewer.tsx` não existe mais no repo; nenhuma referência residual a ele em `src/`.
- `FlashcardReviewSession.tsx` referencia o caminho atômico via `submit_flashcard_review`; `syncHandlers.ts:167` chama `supabase.rpc('submit_flashcard_review', ...)`.
- `tests/e2e/specs/concurrencia-13b.spec.ts` existe, com exatamente 7 `test(...)` distribuídos em 3 `describe` (reação/nota/progresso; SRS de flashcard concorrente + retry idempotente; simulado rascunho + finalização idempotente) — bate exatamente com a descrição do retorno. Usa `browser`/`newContext` para os cenários de duas abas reais.
- `docs/diretoria/registro.md` tem a entrada "Retorno recebido — 13-B, 2026-09-12"; `AGENTS.md` marca "Prompt 13-B ... PUBLICADO" e referencia o registro. A dívida técnica da corrida entre abas do mesmo `BrowserContext` está documentada em ambos.

## Não verificado nesta passada (aceito por convergência de evidência, não por execução própria)

- Não reexecutei a suíte completa localmente (pgTAP 183/183, Playwright 17/17) nem o CI do GitHub diretamente — a evidência é a convergência entre histórico de commits, arquivos presentes/ausentes e documentação, sem contradição em nenhum ponto checado.
- Hash de bundle do deploy Vercel e smoke de produção: aceitos pela declaração; não há acesso direto ao ambiente de produção nesta verificação.

## Decisão

ACEITA e PUBLICADA. Nenhuma discrepância encontrada entre o retorno declarado e o estado real do repositório. Libera os candidatos gateados por "retorno e decisão do 13-B" / "fundação de testes publicada" / "testes publicados".

Dívidas técnicas herdadas para acompanhamento, não bloqueiam novos prompts: corrida entre abas do mesmo `BrowserContext` na fila de sync; acessibilidade de teclado do `AdminCMSView`.
